CREATE TABLE IF NOT EXISTS player_matches (
    player_match VARCHAR(17) NOT NULL,
    pid VARCHAR(8),
    match_id VARCHAR(8),
    tls_id VARCHAR(17),
    team_match VARCHAR(17),
    season_long VARCHAR(12),
    date DATE,
    start VARCHAR(1),
    position VARCHAR(3),
    min INT,
    gls INT,
    ast INT,
    pk_made INT,
    pk_att INT,
    sh INT,
    sot INT,
    xg REAL,
    npxg REAL,
    xag REAL,
    sca INT,
    gca INT,
    pass_cmp_t INT,
    pass_att_t INT,
    pass_cmp_pct_t REAL,
    pass_totdist REAL,
    pass_prgdist REAL,
    pass_cmp_s INT,
    pass_att_s INT,
    pass_cmp_pct_s REAL,
    pass_cmp_m INT,
    pass_att_m INT,
    pass_cmp_pct_m REAL,
    pass_cmp_l INT,
    pass_att_l INT,
    pass_cmp_pct_l REAL,
    xa REAL,
    key_passes INT,
    pass_fthird INT,
    pass_pena INT,
    cross_pena INT,
    pass_prg INT,
    pass_live INT,
    pass_dead INT,
    pass_fk INT,
    through_balls INT,
    switches INT,
    crosses INT,
    throw_ins INT,
    corners INT,
    pass_block INT,
    tkl INT,
    tkl_won INT,
    tkl_def3 INT,
    tkl_mid3 INT,
    tkl_att3 INT,
    tkl_drb INT,
    tkl_drb_att INT,
    tkl_drb_pct REAL,
    tkl_drb_lost INT,
    blocks INT,
    block_sh INT,
    block_pass INT,
    int INT,
    tkl_plus_int INT,
    clearances INT,
    err_oppsh INT,
    touches INT,
    tch_defpen INT,
    tch_def3 INT,
    tch_mid3 INT,
    tch_att3 INT,
    tch_attpen INT,
    tch_live INT,
    to_att INT,
    to_suc INT,
    to_suc_pct REAL,
    to_tk INT,
    to_tk_pct REAL,
    carries INT,
    car_dist INT,
    car_prgdist INT,
    car_prg INT,
    car_f3 INT,
    car_pen INT,
    car_mis_cont INT,
    car_dis_poss INT,
    pass_rec INT,
    pass_prg_rec INT,
    crd_y INT,
    crd_r INT,
    crd_2y INT,
    fls INT,
    fld INT,
    offside INT,
    pk_won REAL,
    og INT,
    recov INT,
    aer_won INT,
    aer_lost INT,
    aer_pct REAL,
    PRIMARY KEY (player_match),
    FOREIGN KEY (pid) REFERENCES players(pid),
    FOREIGN KEY (match_id) REFERENCES matches(match_id),
    FOREIGN KEY (tls_id) REFERENCES team_league_seasons(tls_id),
    FOREIGN KEY (team_match) REFERENCES team_matches(team_match)
);