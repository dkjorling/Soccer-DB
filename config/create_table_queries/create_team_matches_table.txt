CREATE TABLE IF NOT EXISTS team_matches (
    team_match VARCHAR(17) NOT NULL,
    match_id VARCHAR(8),
    tls_id VARCHAR(17),
    date DATE,
    time TIME,
    round VARCHAR(50),
    tid VARCHAR(8),
    opp_tid VARCHAR(8),
    home_away VARCHAR(1),
    result VARCHAR(3),
    gf INT,
    ga INT,
    so_gf INT,
    so_ga INT,
    xg REAL,
    xga REAL,
    poss REAL,
    formation VARCHAR(12),
    sh INT,
    sot INT,
    sh_dist REAL,
    sh_fk INT,
    pk_made INT,
    pk_att INT,
    npxg REAL,
    pass_cmp_t INT,
    pass_att_t INT,
    pass_cmp_pct_t REAL,
    pass_totdist REAL,
    pass_prgdist REAL,
    pass_cmp_s INT,
    pass_att_s INT,
    pass_cmp_pct_s REAL,
    pass_cmp_m INT,
    pass_att_m INT,
    pass_cmp_pct_m REAL,
    pass_cmp_l INT,
    pass_att_l INT,
    pass_cmp_pct_l REAL,
    ast INT,
    xag REAL,
    xa REAL,
    key_passes INT,
    pass_fthird INT,
    pass_pena INT,
    cross_pena INT,
    pass_prg INT,
    pass_live INT,
    pass_dead INT,
    pass_fk INT,
    through_balls INT,
    switches INT,
    crosses INT,
    throw_ins INT,
    corners INT,
    pass_block INT,
    sca INT,
    sc_passlive INT,
    sc_passdead INT,
    sc_takeon INT,
    sc_shot INT,
    sc_fld INT,
    sc_def INT,
    gca INT,
    gc_passlive INT,
    gc_passdead INT,
    gc_takeon INT,
    gc_shot INT,
    gc_fld INT,
    gc_def INT,
    touches INT,
    tch_defpen INT,
    tch_def3 INT,
    tch_mid3 INT,
    tch_att3 INT,
    tch_attpen INT,
    tch_live INT,
    to_att INT,
    to_suc INT,
    to_suc_pct REAL,
    to_tk INT,
    to_tk_pct REAL,
    carries INT,
    car_dist INT,
    car_prgdist INT,
    car_prg INT,
    car_f3 INT,
    car_pen INT,
    car_mis_cont INT,
    car_dis_poss INT,
    pass_rec INT,
    pass_prg_rec INT,
    tkl INT,
    tkl_won INT,
    tkl_def3 INT,
    tkl_mid3 INT,
    tkl_att3 INT,
    tkl_drb INT,
    tkl_drb_att INT,
    tkl_drb_pct REAL,
    tkl_drb_lost INT,
    blocks INT,
    block_sh INT,
    block_pass INT,
    int INT,
    tkl_plus_int INT,
    clearances INT,
    err_oppsh INT,
    crd_y INT,
    crd_r INT,
    crd_2y INT,
    fls INT,
    fld INT,
    offside INT,
    og INT,
    recov INT,
    aer_won INT,
    aer_lost INT,
    aer_pct REAL,
    sot_ag INT,
    saves INT,
    save_pct REAL,
    clean_sheet INT,
    ps_xg REAL,
    pk_att_ag INT,
    pk_saved INT,
    pk_miss_ag INT,
    pass_launch_cmp INT,
    pass_launch_att INT,
    pass_launch_cmp_pct REAL,
    keep_pass_att INT,
    keep_throw_att INT,
    keep_launch_pct REAL,
    keep_pass_avg_len REAL,
    glkck_att INT,
    glkck_launch_pct REAL,
    glkck_avg_len REAL,
    crosses_ag INT,
    crosses_ag_stp INT,
    crosses_ag_stp_pct REAL,
    def_out_pena INT,
    def_out_pena_avg_dist REAL,
    PRIMARY KEY (team_match),
    FOREIGN KEY (match_id) REFERENCES matches(match_id),
    FOREIGN KEY (tls_id) REFERENCES team_league_seasons(tls_id),
    FOREIGN KEY (tid) REFERENCES teams(tid)
);