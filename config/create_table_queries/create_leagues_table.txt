CREATE TABLE IF NOT EXISTS leagues (
            lg_id INTEGER NOT NULL PRIMARY KEY,
            lg_name TEXT,
            type TEXT,
            comp_tier TEXT,
            big_five INTEGER,
            api_id INTEGER,
            country_code TEXT,
            logo_url TEXT
        );